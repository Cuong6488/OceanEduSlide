
@{
    ViewBag.Title = "Thêm mới chi nhánh";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
<h2 class="mt-0">Thêm mới chi nhánh</h2>
<a class="btn quick-link" href="@Url.Action("ListOffice")"><i class="far fa-list me-1"></i> Danh sách chi nhánh</a>
<a class="btn quick-link" data-fancybox data-type="iframe" data-src="@Url.Action("ListFile","Revenue", new {type = 3})" href="javascript:;">
    <i class="far fa-list me-1"></i> Lịch sử cập nhật
</a>
<div class="box_content">
    @using (Html.BeginForm("InsertOfficeExcel", "Vcms", FormMethod.Post, new { enctype = "multipart/form-data" }))
    {
        @Html.ValidationSummary(true)
        <table class="form_table">
            <tr>
                <td class="form_name">Đính kèm File</td>
                <td class="form_text"><input type="file" name="OfficeFile" value="" /></td>
            </tr>
            <tr>
                <td class="form_name"></td>
                <td class="form_text">
                    <input type="submit" class="btn quick-link" value="Xác nhận" />
                </td>
            </tr>
        </table>
    }
</div>